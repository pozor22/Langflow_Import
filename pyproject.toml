[project]
name = "lfdocker"
version = "0.1.0"
description = ""
authors = [
    {name = "Pozor",email = "shaginov1b@yandex.ru"}
]
readme = "README.md"
requires-python = ">=3.12,<4.0"
dependencies = [
    "langchain-gigachat (>=0.3.12,<0.4.0)",
    "gigachat (>=0.1.43,<0.2.0)",
    "telebot (>=0.0.5,<0.0.6)",
    "aiohttp (>=3.13.2,<4.0.0)",
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
